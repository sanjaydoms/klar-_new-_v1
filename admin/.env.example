# The Integration Control Center API.
VITE_INTEGRATION_URL=http://localhost:5022
# Auth service — this app signs in against the same accounts as the rest of KLAR.
VITE_AUTH_URL=http://localhost:5010

# Local development without the shared Atlas cluster: point this at the auth
# stub instead (`npm run stubs` from the repository root), which signs in any
# credentials as a MASTER user.
#   VITE_AUTH_URL=http://localhost:5910
