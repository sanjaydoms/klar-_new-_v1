node_modules/
dist/
coverage/
*.tsbuildinfo

# Local environment. Supplier credentials come from configuration management,
# never from a committed file — see docs/adr/0004-supplier-contract.md.
.env
.env.*
!.env.example

# The reference implementation, extracted for Phase 1 analysis only. Nothing in
# src/ imports from it and it is not part of the build.
_reference/

.DS_Store
Thumbs.db

# Local Claude Code session config (permission allowlists, etc.) — not project source.
.claude/

# The repo root ignores `*.md` (audit-file cleanup). This engine's README and
# docs/adr are hand-written design rationale, not audit output — keep them.
!*.md
